apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: applications-default-deny-ingress
  namespace: applications
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
