apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - helm-release.yaml
  - pool.yaml

commonLabels:
  app.kubernetes.io/part-of: metallb
