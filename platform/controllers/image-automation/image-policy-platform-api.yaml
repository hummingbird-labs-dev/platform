apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: platform-api
  namespace: image-automation
spec:
  imageRepositoryRef:
    name: platform-api
  policy:
    semver:
      range: '*'
