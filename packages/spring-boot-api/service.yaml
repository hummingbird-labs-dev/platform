apiVersion: v1
kind: Service
metadata:
  name: spring-boot-api
  labels:
    app.kubernetes.io/component: api
    app.kubernetes.io/name: spring-boot-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spring-boot-api
  ports:
    - name: http
      port: 8080
      targetPort: http
