apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: deployments
  namespace: flux-system
spec:
  dependsOn:
    - name: platform-baselines
  interval: 10m
  path: ./deployments
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
