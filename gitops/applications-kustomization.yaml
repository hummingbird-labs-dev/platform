apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: applications
  namespace: flux-system
spec:
  dependsOn:
    - name: platform-baselines
  interval: 10m
  path: ./applications
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
